# 冒号之后要跟空格
#eureka相关配置
server:
  port: 8761

#不注册自己；不将Eureka Server作为客户端注册
#不同步其它注册中心
#默认路径
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka/






